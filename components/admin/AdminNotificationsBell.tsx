"use client";

/**
 * Stub — in-app obaveštenja (notifications tabela) nisu u MVP šemi.
 * Zadržava mesto u top baru radi buduće integracije.
 */
export default function AdminNotificationsBell() {
  return (
    <span className="admin-template-link-btn" style={{ opacity: 0.65, cursor: "default" }} title="Obaveštenja">
      —
    </span>
  );
}
